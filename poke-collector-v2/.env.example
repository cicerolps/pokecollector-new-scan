# Docker user mapping (homelab convention)
PUID=1000
PGID=1000
TZ=Etc/UTC

# Optional — raises the pokemontcg.io free-tier rate limit when set.
# Leave empty to use the unauthenticated tier.
POKEMONTCG_API_KEY=
