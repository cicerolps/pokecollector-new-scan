# Docker user mapping (homelab convention)
PUID=1000
PGID=1000
TZ=Etc/UTC

# Optional, currently unused by the sync job (it now uses tcgdex.dev, which
# needs no key — pokemontcg.io's free API became unreliable after its team
# moved to the commercial Scrydex product). Kept in case the pokemontcg.io
# client is useful again later.
POKEMONTCG_API_KEY=
